[project]
name = "mping"
version = "0.1.0"
description = ""
requires-python = ">=3.14"
dependencies = []

[project.scripts]
mping = "mping.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mping"]
